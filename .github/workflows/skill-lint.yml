name: skill-lint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint skills
        run: bash tools/skill-lint.sh
      - name: Syntax-check shell scripts
        run: bash -n install.sh tools/skill-lint.sh tools/install-prometheus-cycle.sh tests/test-prometheus-cycle.sh tests/fixtures/fake-openclaw.sh
      - name: shellcheck
        run: shellcheck install.sh tools/skill-lint.sh tools/install-prometheus-cycle.sh tests/test-install.sh tests/test-brain.sh tests/test-prometheus-cycle.sh tests/fixtures/fake-openclaw.sh
      - name: Prometheus learning-cycle contract
        run: bash tests/test-prometheus-cycle.sh
      - name: Installer test matrix (sandbox HOME)
        run: bash tests/test-install.sh
      - name: Brain protocol live proof (two clones, bare remote)
        run: bash tests/test-brain.sh
