interface:
  display_name: "Reflect"
  short_description: "Turn completed work into durable Prometheus improvements"
  default_prompt: "Use $reflect to extract verified lessons, decisions, and project-status changes from this work, deduplicate them, and persist only durable improvements to Prometheus."

policy:
  allow_implicit_invocation: true
