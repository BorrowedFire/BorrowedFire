interface:
  display_name: "Ship"
  short_description: "Commit, merge, deploy, and verify"
  default_prompt: "Use $ship to commit, merge, deploy, and verify the current work."

policy:
  allow_implicit_invocation: true
