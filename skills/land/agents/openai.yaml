interface:
  display_name: "Land"
  short_description: "Drive one PR through review to a clean, proven merge"
  default_prompt: "Use $land to take the current branch through adversarial + Codex review, auto-fix, live proof, and merge-when-clean — surfacing only a decision-ready ask if it cannot finish."

policy:
  allow_implicit_invocation: true
